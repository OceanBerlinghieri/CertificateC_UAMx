/*#include <stdio.h>

int main() {
	double total = 0, price = 0;
	int hours;
	printf("Think about the last 5 games you have played\n");

	printf("How many hours you played the first one?: ");
	scanf_s("%d", &hours);
	printf("Which was the cost?: ");
	scanf_s("%lf", &price);

	total += (price / hours);

	printf("How many hours you played the second one?: ");
	scanf_s("%d", &hours);
	printf("Which was the cost?: ");
	scanf_s("%lf", &price);

	total += (price / hours);

	printf("How many hours you played the third one?: ");
	scanf_s("%d", &hours);
	printf("Which was the cost?: ");
	scanf_s("%lf", &price);

	total += (price / hours);

	printf("How many hours you played the fourth one?: ");
	scanf_s("%d", &hours);
	printf("Which was the cost?: ");
	scanf_s("%lf", &price);

	total += (price / hours);

	printf("How many hours you played the fifth one?: ");
	scanf_s("%d", &hours);
	printf("Which was the cost?: ");
	scanf_s("%lf", &price);

	total += (price / hours);

	printf("The last five games cost you %lf per hour", total/5);
	return 0;
}*/