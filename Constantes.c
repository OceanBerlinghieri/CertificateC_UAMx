/*#include <stdio.h>

int main(){
    int ancho, largo;
    double ancho_p, largo_p;
    const double pies_por_cm = 0.0328;
    
    //Las constantes se declaran dentro del main y se escriben como
    //una instrucción normal del programa. No se podrá modificar el valor de la
    //constante mediante la asignación. Las maneja el compilador y pueden
    //usarse punteros con ellas o hacerles un cast.
    
    printf("Introduce el ancho: ");
    scanf("%d", &ancho);
    ancho_p = ancho * pies_por_cm;
    
    printf("Introduce el largo: ");
    scanf("%d", &largo);
    largo_p = largo * pies_por_cm;
    
    printf("%d x %d cm son %.2lf x %.2lf pies.\n", ancho, largo, ancho_p, largo_p);
    
    return 0;
}*/
