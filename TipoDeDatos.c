/*#include <stdio.h>

int main() {
	char sex;
	int cp, hp, candy;
	double weight, height;

	printf("Intrduce your sex,,cp, hp, weight, height and candy\n");
	scanf_s("%c", &sex, 2);
	scanf_s("%d", &cp);
	scanf_s("%d", &hp);
	scanf_s("%lf", &weight);
	scanf_s("%lf", &height);
	scanf_s("%d", &candy);

	printf("Sex: %d\nCP: %d\nHP: %d\nWeight: %d\nHeight: %d\nCandy: %d", sizeof(sex), sizeof(cp), sizeof(hp), sizeof(weight), sizeof(height), sizeof(candy));

	return 0;
}*/