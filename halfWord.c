/*#include <stdio.h>

#define MAX_STRING 256
int main(){
    int num, i;
    
    printf("Intruduce a number: ");
    scanf("%d", &num);
    
    for(i = 0; i <= 10; i++){
        printf("%d per %d is %d\n", num, i, num*i);
    }
    
    return 0;
}

int main() {
    char string[MAX_STRING];
    
    printf("Introduce a string: ");
    gets(string);
    
    printf("The string is %d characters long and half word is in position %d\n", 
    strlen(string), strlen(string)/2);
    
    printf("The second half of the string is: ");
    for(int i = strlen(string)/2; i<strlen(string);i++){
        printf("%c", string[i]);
    }
    return 0;
}*/