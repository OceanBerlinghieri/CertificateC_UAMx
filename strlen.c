/*#include <stdio.h>

#define MAX 256

int main() {
    char s[MAX], *p;
    int i;
    
    printf("Introduce a string: ");
    gets(s);
    
    for(i = 0, p = s; *p != 0; i++, p++);
    
    printf("%s has %d characters", s, i);
    
}*/